<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.moutamid.sampoorankranti">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".ContextSampooran"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NeedHelpFormActivity"></activity>
        <activity android:name=".AdminDashboardActivity" />
        <activity android:name=".ChatsListActivity" />
        <activity android:name=".UsersListActivity" />
        <activity android:name=".NotoficationActivity" />
        <activity android:name=".PublicPollActivity" />
        <activity android:name=".AdvertiseActivity" />
        <activity android:name=".InfoActivity" />
        <activity android:name=".ConversationActivity" />
        <activity android:name=".FilterChatActivity" />
        <activity android:name=".SliderImageViewerActivity" />
        <activity android:name=".DashboardActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SecondRegistrationActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>